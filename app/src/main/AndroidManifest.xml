<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phonenumbertracker">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_two"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_two_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.PhoneNumberTracker">
        <activity
            android:name=".activities.ContactDetailsActivity"
            android:label="@string/title_activity_contact_details"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ContactListActivity"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar" />
        <activity
            android:name=".activities.SearchNumberActivity"
            android:label="@string/title_activity_search_number"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.USSDCodeDetailsActivity"
            android:label="@string/title_activity_u_s_s_d_code_details"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.ListOfContacts"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.TrafficFinderActivity"
            android:label="@string/title_activity_traffic_finder"
            android:theme="@style/Theme.PhoneNumberTracker"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.NearPlacesActivity"
            android:label="@string/title_activity_near_places"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.USSDCodeActivity"
            android:label="@string/title_activity_u_s_s_d_code"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.CompassActivity"
            android:label="@string/title_activity_compass"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.SpeedoMeterActivity"
            android:label="@string/title_activity_speedo_meter"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAKEMdBd80dVUq8exWJO_jfVOdPxI73M08" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.LiveLocationActivity"
            android:label="@string/title_activity_live_location"
            android:theme="@style/Theme.PhoneNumberTracker"
            android:screenOrientation="portrait"/>
        <!--
        <activity
            android:name=".activities.ContactsActivity"
            android:label="@string/title_activity_contacts"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>-->
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/title_activity_second"
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AreaCodeActivity"
            android:label=""
            android:theme="@style/Theme.PhoneNumberTracker.NoActionBar"
            android:screenOrientation="portrait"/>
    </application>

</manifest>